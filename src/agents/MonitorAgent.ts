import { URLCollection } from '../types';

/**
 * MonitorAgent tracks changes in collections and emits events.
 */
export class MonitorAgent {
  async watch(collection: URLCollection): Promise<void> {
    // placeholder for monitoring logic
  }
}
